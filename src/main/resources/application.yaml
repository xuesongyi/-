spring:
  messages:
    basename: i18h.login
  datasource:
    # 数据库用户名和密码
    username: system
    password: xiaoao666
    url: jdbc:oracle:thin:@127.0.0.1:1521:ORCL
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: oracle.jdbc.driver.OracleDriver
    druid:
      initialSize: 5
      minIdle: 5
      maxActive: 20
      maxWait: 60000
      timeBetweenEvictionRunsMillis: 60000
      minEvictableIdleTimeMillis: 300000
      validationQuery: SELECT 1 FROM DUAL
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      poolPreparedStatements: true
      filters: stat,wall,log4j
      maxPoolPreparedStatementPerConnectionSize: 20
      useGlobalDataSourceStat: true
      connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
# 热部署
  thymeleaf:
    cache: false
  freemarker:
    cache: false
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
      exclude: static/**
#发送验证邮件
  mail:
    protocol: smtp
    host: smtp.qq.com
    port: 465
    #自己邮箱和协议密码
    username: 2239699359@qq.com
    password: opewfnecxmtnechi
    default-encoding: utf-8
    from: 2239699359@qq.com
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            enable: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
# 整合mybatis
mybatis:
  type-aliases-package: com.example.springweb.pojo
  mapper-locations: /mapper/*.xml
server:
  tomcat:
    uri-encoding: utf-8
  port: 8015
# 日志
logging:
  level:
    root: info
    org.springframework.web: debug
  file:
    path: /www/wwwroot/default/log
# 分页插件配置
pagehelper:
  helper-dialect: oracle
  reasonable: true
  support-methods-arguments: true
  params: count=countSql
# 自定义参数
#邮件验证路径
severUrl: "http://127.0.0.1:8015/validMail?confirmCode="
#图片上传路径
imgFile: "F://uploads/images/"


